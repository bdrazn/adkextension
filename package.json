{
  "name": "adk-chat-deploy",
  "version": "0.1.0",
  "private": true,
  "description": "Build VSIX and deploy ADK Chat server to Vercel",
  "scripts": {
    "build:vsix": "node scripts/build-vsix.mjs",
    "build:server": "node scripts/prepare-vercel-server.mjs",
    "deploy": "npm run build:server && vercel",
    "vercel": "vercel"
  },
  "dependencies": {
    "@google/adk": "^0.3.0",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@vscode/vsce": "^2.22.0"
  }
}
